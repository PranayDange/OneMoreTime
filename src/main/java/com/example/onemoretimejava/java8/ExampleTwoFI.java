package com.example.onemoretimejava.java8;

public interface ExampleTwoFI {
   // abstract int sum(int a, int b);
    int sum(int a, int b);
    //in this if you don't add abstract compiler will itself add public and abstract
}
